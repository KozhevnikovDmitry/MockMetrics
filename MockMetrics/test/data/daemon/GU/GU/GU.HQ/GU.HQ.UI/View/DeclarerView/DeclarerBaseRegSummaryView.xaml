<UserControl x:Class="GU.HQ.UI.View.DeclarerView.DeclarerBaseRegSummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ViewCommon="clr-namespace:Common.UI.View;assembly=Common.UI" 
             mc:Ignorable="d" >
    
    <StackPanel Orientation="Vertical">
        <ViewCommon:ListView  DataContext="{Binding Path=DeclarerBaseRegVm}"/>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock    Text="Иное:" />
            <TextBox      Text="{Binding Path=OtherBaseReg}"/>
            
        </Grid>
    </StackPanel>
</UserControl>
