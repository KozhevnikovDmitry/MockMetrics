<UserControl x:Class="GU.HQ.UI.View.ClaimView.ClaimShortView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="150" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontWeight="Bold" Text="Дата подачи:"/>
        <TextBlock Grid.Row="1" FontWeight="Bold" Text="Статус:"/>
        <TextBlock Grid.Row="2" FontWeight="Bold" Text="Заявитель:"/>

        <TextBlock Grid.Row="0" Grid.Column="1" TextWrapping="WrapWithOverflow" Text="{Binding Path=ClaimDate}"/>
        <TextBlock Grid.Row="1" Grid.Column="1" TextWrapping="WrapWithOverflow" Text="{Binding Path=ClaimStatus}"/>
        <TextBlock Grid.Row="2" Grid.Column="1" TextWrapping="WrapWithOverflow" Text="{Binding Path=ClaimDeclarerFio}"/>
    </Grid>
</UserControl>
