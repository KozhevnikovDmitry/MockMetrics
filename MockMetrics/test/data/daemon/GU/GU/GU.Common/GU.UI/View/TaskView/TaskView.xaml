<UserControl x:Class="GU.UI.View.TaskView.TaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:v="clr-namespace:GU.UI.View.TaskView"
             xmlns:View="clr-namespace:Common.UI.View;assembly=Common.UI" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding LastChangesStamp}"
                   Foreground="Gray" FontStyle="Italic"
                   HorizontalAlignment="Right" Grid.Row="0" />

        <DockPanel Grid.Row="1">
            <GroupBox Header="{Binding TaskTitle}" DockPanel.Dock="Top">
                <v:TaskCommonView DataContext="{Binding TaskDataVM}"/>
            </GroupBox>
            <View:ListView DataContext="{Binding TaskStatusesVM}" DockPanel.Dock="Bottom"/>
        </DockPanel>
    </Grid>
</UserControl>
