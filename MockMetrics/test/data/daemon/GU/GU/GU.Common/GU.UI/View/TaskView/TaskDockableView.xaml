<UserControl x:Class="GU.UI.View.TaskView.TaskDockableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:guview="clr-namespace:GU.UI.View.TaskView" 
             xmlns:contentView="clr-namespace:GU.UI.View.ContentView"
             xmlns:taskViewModel="clr-namespace:GU.UI.ViewModel.TaskViewModel"
             d:DataContext="{d:DesignInstance taskViewModel:TaskDockableVM}"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolVisibilityConverter" />
    </UserControl.Resources>
    <TabControl>

        <TabItem Header="Информация по заявке">
            <guview:TaskView DataContext="{Binding TaskVM}" />
        </TabItem>

        <!--TabItem Header="Документы">
            <guview:TaskDocManagerView DataContext="{Binding TaskDocumentsManagerVM}"/>
        </TabItem-->

        <TabItem Header="Заявление" Visibility="{Binding HasContent, Converter={StaticResource BoolVisibilityConverter}}">
            <ScrollViewer >
                <contentView:ContentNodeView DataContext="{Binding ContentNodeVM}"/>
            </ScrollViewer>
        </TabItem>

    </TabControl>

</UserControl>