<UserControl x:Class="GU.UI.View.TaskView.TaskDocManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:v="clr-namespace:GU.UI.View.TaskView"
             xmlns:ValueConverters="clr-namespace:Common.UI.ViewModel.ValueConverters;assembly=Common.UI"
             xmlns:View="clr-namespace:Common.UI.View;assembly=Common.UI" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <UserControl.Resources>
        <ValueConverters:BoolVisibilityConverter x:Key="boolVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid DockPanel.Dock="Bottom">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>


        <ContentControl Grid.Column="0" Margin="4" >
            <View:ListView DataContext="{Binding TaskDocsVM}"/>
        </ContentControl>
        <ContentControl Grid.Column="1" Margin="4" Visibility="{Binding IsTaskDocManaging, Converter={StaticResource boolVisibilityConverter}}">
            <v:TaskDocView DataContext="{Binding TaskDocVM}"/>
        </ContentControl>

    </Grid>
</UserControl>
