<UserControl x:Class="GU.Archive.UI.View.PostView.PostExecutorItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Border Style="{StaticResource ResourceKey=SearchItemBorderStyle}">
        <WrapPanel Orientation="Horizontal">
            <Border Style="{StaticResource ResourceKey=MainBorderStyle}">
                <StackPanel>
                    <DockPanel LastChildFill="True" Width="600">
                        <TextBlock Text="{Binding UserName}" DockPanel.Dock="Right" Margin="0,0,4,0"
                                   Foreground="Gray" FontStyle="Italic"/>
                        <TextBlock Text="{Binding ExecutorString}" TextWrapping="Wrap"/>
                    </DockPanel>
                    <TextBlock Text="{Binding Note}" Margin="15,5,5,5" Width="500" TextWrapping="Wrap" FontStyle="Italic" FontSize="10"/>
                </StackPanel>
            </Border>
        </WrapPanel>
    </Border>
</UserControl>
