<UserControl x:Class="GU.Archive.UI.View.PostView.PostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:avp="clr-namespace:GU.Archive.UI.View.PostView"
             xmlns:View="clr-namespace:Common.UI.View;assembly=Common.UI" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0">
            <GroupBox Header="Информация" Margin="5">
                <AdornerDecorator>
                    <StackPanel>
                        <avp:PostCommonView DataContext="{Binding PostDataVM}"/>
                    </StackPanel>
                </AdornerDecorator>
            </GroupBox>
        </WrapPanel>

        <Button Content="Добавить ответственного" Command="{Binding AddExecutorCommand}"
                Margin="10 2 2 2" Grid.Row="1" Width="400" HorizontalAlignment="Left" />

        <View:ListView DataContext="{Binding PostExecutorsVM}" Grid.Row="2" Margin="3"/>

    </Grid>
</UserControl>
